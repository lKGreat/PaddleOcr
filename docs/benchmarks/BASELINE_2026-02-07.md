# Benchmark Baseline (2026-02-07)

Environment:
- Workspace: `E:\codeding\PaddleOcr`
- Runtime: `.NET 10` Release build
- Command runner: `pocr benchmark run`

## Scenario: `train:train`

Command:

```bash
pocr benchmark run --scenario train:train \
  -c assets/configs/local/train_bench_rec_ci_fast.yml \
  --warmup 0 --iterations 1 \
  --report_json benchmark_results/baseline_train_train.json
```

Result:
- avg_ms: `879.6594`
- p95_ms: `879.6594`
- throughput_per_sec: `1.1368`

## Scenario: `train:train` (DET fixture)

Command:

```bash
pocr benchmark run --scenario train:train \
  -c assets/configs/local/train_bench_det_ci_fast.yml \
  --warmup 0 --iterations 1 \
  --report_json benchmark_results/baseline_train_det.json
```

Result:
- avg_ms: `2749.9418`
- p95_ms: `2749.9418`
- throughput_per_sec: `0.36364`

## Scenario: `e2e:eval`

Command:

```bash
pocr benchmark run --scenario e2e:eval \
  --gt_dir <tmp_gt_dir> --pred_dir <tmp_pred_dir> \
  --warmup 0 --iterations 2 \
  --report_json benchmark_results/baseline_e2e_eval.json
```

Result:
- avg_ms: `6.26135`
- p95_ms: `11.601905`
- throughput_per_sec: `159.70997`

## Notes

- `service:test` baseline depends on external service endpoint availability and should be captured in deployment environment.
